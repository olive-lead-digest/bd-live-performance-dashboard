// P2-2 — /reporting renamed to /analytics; the page is titled "Trends" in the
// nav and on screen. The old route 301-redirects here (see next.config
// redirects) and the URL is deliberately unchanged.
export { default } from '@/app/reporting/page';
