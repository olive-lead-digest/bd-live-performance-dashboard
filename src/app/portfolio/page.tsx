// P2-2 — /performance renamed to /portfolio; the page is titled "Signings &
// Revenue" in the nav and on screen. The old route 301-redirects here (see
// next.config redirects) and the URL is deliberately unchanged.
export { default } from '@/app/performance/page';
